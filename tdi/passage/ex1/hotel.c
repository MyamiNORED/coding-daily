#include<stdio.h>

struct Chambre {
	int Nemuro;
	int NmbreLits;
	char Etat;
	float Prix;
	}


struct Chambre T[100];
void Ajouter(N){
	int nemuro;
	printf("entrer le nemuro de chambre a ajouter");
	scanf("%d",&num);
	for (int i =0 ; i < N; i++){
		
	if (num<T[i]) { 
	T[i] = 
	for (j = i; j < N+1; j++) {


main(){

if(choix == 1) {Afficher()};

if(choix == 2) {Ajouter()};

if(choix == 1) {Supprimer()};


if(choix == 1) {Modifier()};
if(choix == 1) {Souvegarder()};

}
